{
  // Type-level tests (no emit): typed feature codes through module augmentation.
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "baseUrl": "../..",
    "paths": { "@meteroid/react": ["src/index.ts"] }
  },
  "include": ["./*.ts", "./*.tsx"]
}
