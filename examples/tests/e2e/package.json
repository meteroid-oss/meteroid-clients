{
  "name": "scribe-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Browser-side end-to-end checks for the Scribe demo. Nightly, against a live sandbox.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "list": "playwright test --list",
    "typecheck": "tsc --noEmit",
    "browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/node": "22.10.2",
    "typescript": "5.9.3"
  }
}
