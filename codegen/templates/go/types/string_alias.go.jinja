{% set type_name = type.name | to_upper_camel_case -%}
package meteroid

{{ doc_comment }}
{#- A true alias (`=`) rather than a defined type, so callers can pass plain
    strings wherever an ID is expected. -#}
type {{ type_name }} = string
